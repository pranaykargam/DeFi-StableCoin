// mkdir DeFi-StableCoin
// cd DeFi-StableCoin
// forge init

`forge install OpenZeppelin/openzeppelin-contracts`

// forge coverage
// forge coverage --report debug

// forge remappings


==>> this is how can we find the exact path of the file command ;-

 `find lib/chainlink-brownie-contracts -name "AggregatorV3Interface.sol" -type f`

 forge test -vv

  foge test --gas-report
  forge coverage --report